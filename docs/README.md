# Repair examples

The current sample006 example is the [fresh V12 full-output comparison](repair04-v12-srj33-sample006-corrected.svg), with [measurements](repair04-v12-srj33-sample006-corrected-metrics.json), an [independent raw-output audit](sample006-two-pass-v12-audit.json), and a [native visual review](sample006-v12-visual-review.json). Both expanded DRC policies change from one error to zero, all ten physical via identities remain exact, and only one trace changes. This is an individual example, not a complete dataset result.

`repair04-sample006-corrected.svg` and its metrics file are compatibility aliases for this reviewed V12 example. The versioned names identify the measured source explicitly.

The old `sample006-bounded-repair.svg` is retained as historical evidence only. Its local checker omitted dedicated via-pad checks; its zero-error claim is withdrawn and it must not be used as current repair or manufacturing evidence.
